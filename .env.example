# Copy to .env and fill in. Never commit .env.
GEMINI_API_KEY=
GROQ_API_KEY=
MISTRAL_API_KEY=

# Model used as the LLM judge for benchmark scoring (must be one of the registry keys)
JUDGE_MODEL=gemini-2.5-flash
